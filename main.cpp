#include"declaraciones.hpp"
int main()
{
int n, R;
std::cin >> n;
std::cin >> R;
std::vector<int> w(n,0);
std::vector<int> r(n,0);
for(int i = 0; i < n; i++){ 
    std::cin >> w[i];
    std::cin >> r[n];
}

}